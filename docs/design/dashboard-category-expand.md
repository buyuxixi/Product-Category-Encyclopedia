# 总览品类：子品类从父卡展开

## 背景

品类概览中子品类与一级品类同级铺开，仅靠浅色虚线/父名区分，辨识度不足。

## 方案

- 网格只渲染一级品类
- 有子类的父卡 footer 提供「N 个子品类 ▾」，点击展开/收起（`stopPropagation`，不跳转百科）
- 子卡嵌在父组内：左缩进色条 + 浅绿底 +「子品类」徽章 + 略小图表/字号
- 点父/子卡主体仍进入对应品类

## 默认

子品类默认收起，首屏更干净。
